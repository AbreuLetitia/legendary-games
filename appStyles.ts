import { StyleSheet } from 'react-native';
import { height, width } from './src/components/constants/Responsive';

export const styles = StyleSheet.create({
  appStyle: {
    flex: 1,
    // width: width(100),
    // height: height(100),
    // paddingHorizontal: width(5),
  },
});
